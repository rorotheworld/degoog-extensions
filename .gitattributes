# Degoog clones this repo into a Linux container. Keep everything LF in the repo
# regardless of the checkout platform, so extension code is never delivered with
# CRLF line endings.
* text=auto eol=lf
*.png binary
